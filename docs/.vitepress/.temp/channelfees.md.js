import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Channel Fees","description":"","frontmatter":{},"headers":[],"relativePath":"channelfees.md","filePath":"channelfees.md","lastUpdated":1773788451000}');
const _sfc_main = { name: "channelfees.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="channel-fees" tabindex="-1">Channel Fees <a class="header-anchor" href="#channel-fees" aria-label="Permalink to &quot;Channel Fees&quot;">​</a></h1><p>The Channel Fees screen is the current channel operations view. It exists to answer one question, what is the operator charging now, what is the channel doing now, and what should change next.</p><h2 id="what-the-screen-shows" tabindex="-1">What the screen shows <a class="header-anchor" href="#what-the-screen-shows" aria-label="Permalink to &quot;What the screen shows&quot;">​</a></h2><ol><li><p>Active channels and peer identity.</p></li><li><p>Outbound fee policy on the operator side.</p></li><li><p>Inbound fee context from the remote side when available.</p></li><li><p>Local and remote liquidity split.</p></li><li><p>Recent forwarding and fee earnings.</p></li><li><p>Supporting peer fee context and network averages where available.</p></li></ol><h2 id="why-this-screen-matters" tabindex="-1">Why this screen matters <a class="header-anchor" href="#why-this-screen-matters" aria-label="Permalink to &quot;Why this screen matters&quot;">​</a></h2><p>Fee changes are simple to apply and hard to evaluate. A channel can be idle for structural reasons, not because the current fee is too high. A channel can also look healthy on volume but still lose opportunity because liquidity is parked on the wrong side. The fee view pulls these signals together before the operator changes policy.</p><h2 id="data-used-by-the-screen" tabindex="-1">Data used by the screen <a class="header-anchor" href="#data-used-by-the-screen" aria-label="Permalink to &quot;Data used by the screen&quot;">​</a></h2><p>For a selected channel the current page builds a narrow working set.</p><ol><li><p>Node info</p></li><li><p>The selected channel</p></li><li><p>Matching peer records</p></li><li><p>Forwarding history</p></li><li><p>Mission control pairs</p></li><li><p>Current fee policy</p></li><li><p>Peer fee averages when available</p></li></ol><h2 id="current-local-preparation-flow" tabindex="-1">Current local preparation flow <a class="header-anchor" href="#current-local-preparation-flow" aria-label="Permalink to &quot;Current local preparation flow&quot;">​</a></h2><p>The page builds four layers before recommendation.</p><ol><li>Raw telemetry</li></ol><p>This is the direct node data needed for the selected channel.</p><ol start="2"><li>Raw snapshot metadata</li></ol><p>This is a local record of collection time, node alias, channel id, peer key, and the raw objects used for the run.</p><ol start="3"><li>Normalized snapshot</li></ol><p>The page passes the selected channel data through <code>normalizeSnapshot</code>. Even a one channel run uses the same normalization contract as wider product flows.</p><ol start="4"><li>Privacy transformed payload</li></ol><p>The page applies <code>applyPrivacyPolicy(normalizedSnapshot, &#39;feature_only&#39;)</code>. This creates the reduced payload used for review and verified runtime execution.</p><h2 id="what-enters-the-normalized-channel-state" tabindex="-1">What enters the normalized channel state <a class="header-anchor" href="#what-enters-the-normalized-channel-state" aria-label="Permalink to &quot;What enters the normalized channel state&quot;">​</a></h2><p>The normalized channel object carries the fields the scoring layer needs.</p><ol><li><p>Channel id</p></li><li><p>Remote pubkey</p></li><li><p>Active flag</p></li><li><p>Capacity</p></li><li><p>Local balance and remote balance</p></li><li><p>Local balance ratio and remote balance ratio</p></li><li><p>Outbound fee ppm and inbound fee ppm</p></li><li><p>Forward counts</p></li><li><p>Revenue</p></li><li><p>Forwarding earning ppm</p></li><li><p>Failed forward count</p></li><li><p>Last activity timestamp</p></li><li><p>Peer centrality</p></li><li><p>Mission success and failure rates</p></li><li><p>Network average fee context when available</p></li></ol><h2 id="what-leaves-the-page-on-the-private-path" tabindex="-1">What leaves the page on the private path <a class="header-anchor" href="#what-leaves-the-page-on-the-private-path" aria-label="Permalink to &quot;What leaves the page on the private path&quot;">​</a></h2><p>The preferred recommendation payload is the <code>feature_only</code> privacy transform.</p><p>That payload keeps the useful channel level signals and removes direct identifiers and raw balance amounts.</p><ol><li><p>Channels become stable references such as <code>channel_0001</code>.</p></li><li><p>Peers become stable references such as <code>peer_0001</code>.</p></li><li><p>Liquidity is sent as ratios, not raw sat amounts.</p></li><li><p>Activity, revenue, and reliability signals are preserved.</p></li><li><p>Raw pubkeys are not sent in the transformed state.</p></li></ol><h2 id="current-execution-modes" tabindex="-1">Current execution modes <a class="header-anchor" href="#current-execution-modes" aria-label="Permalink to &quot;Current execution modes&quot;">​</a></h2><h3 id="standard-mode" tabindex="-1">Standard mode <a class="header-anchor" href="#standard-mode" aria-label="Permalink to &quot;Standard mode&quot;">​</a></h3><p>The current Channel Fees page still supports the generic <code>/api/recommend</code> path in standard mode. The page also prepares the reduced payload locally so the operator can inspect the intended private request structure.</p><h3 id="verified-runtime-mode" tabindex="-1">Verified runtime mode <a class="header-anchor" href="#verified-runtime-mode" aria-label="Permalink to &quot;Verified runtime mode&quot;">​</a></h3><p>In verified runtime mode the page sends the reduced payload to the verified service path and then receives recommendation, attestation, and verification material.</p><h2 id="recommendation-output" tabindex="-1">Recommendation output <a class="header-anchor" href="#recommendation-output" aria-label="Permalink to &quot;Recommendation output&quot;">​</a></h2><p>The current scoring output can produce three recommendation families. For the fee screen the relevant one is <code>feeRecommendations</code>.</p><p>Each fee recommendation includes:</p><ol><li><p><code>action</code></p></li><li><p><code>currentFeePpm</code></p></li><li><p><code>suggestedFeePpm</code></p></li><li><p><code>confidence</code></p></li><li><p>signal bands for activity, liquidity, reliability, centrality, and revenue</p></li><li><p>human readable reasons</p></li></ol><h2 id="why-the-design-matters" tabindex="-1">Why the design matters <a class="header-anchor" href="#why-the-design-matters" aria-label="Permalink to &quot;Why the design matters&quot;">​</a></h2><p>The fee screen needs local detail, but the external scoring path does not need the full local record. The product therefore separates screen level observability from recommendation payload design. That separation is the practical core of the private pipeline in LN Advisor.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("channelfees.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const channelfees = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  channelfees as default
};
