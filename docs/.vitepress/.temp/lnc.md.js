import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"LNC","description":"","frontmatter":{},"headers":[],"relativePath":"lnc.md","filePath":"lnc.md","lastUpdated":1773788451000}');
const _sfc_main = { name: "lnc.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="lnc" tabindex="-1">LNC <a class="header-anchor" href="#lnc" aria-label="Permalink to &quot;LNC&quot;">​</a></h1><p>LNC means Lightning Node Connect. In LN Advisor it is the session layer between the browser application and the operator node.</p><h2 id="what-lnc-does-in-this-project" tabindex="-1">What LNC does in this project <a class="header-anchor" href="#what-lnc-does-in-this-project" aria-label="Permalink to &quot;What LNC does in this project&quot;">​</a></h2><ol><li><p>It gives the application a live authenticated session to the node.</p></li><li><p>It lets the frontend call LND RPC methods through the active session.</p></li><li><p>It avoids a separate node specific backend just to read the operator state.</p></li><li><p>It keeps the node as the primary data source for the product.</p></li></ol><h2 id="connection-flow-in-the-app" tabindex="-1">Connection flow in the app <a class="header-anchor" href="#connection-flow-in-the-app" aria-label="Permalink to &quot;Connection flow in the app&quot;">​</a></h2><p>The connection screen supports two states.</p><ol><li><p>First connection. The user enters an LNC pairing phrase and a password.</p></li><li><p>Returning session. The user enters only the password for the saved session.</p></li></ol><p>The current connect screen labels this flow clearly and presents it as <code>Connect Your Node</code> on first use and <code>Login</code> for a saved pairing.</p><h2 id="data-read-through-lnc" tabindex="-1">Data read through LNC <a class="header-anchor" href="#data-read-through-lnc" aria-label="Permalink to &quot;Data read through LNC&quot;">​</a></h2><p>LN Advisor reads several classes of data through the active LNC session.</p><ol><li><code>getInfo</code></li></ol><p>This provides node identity and basic node state.</p><ol start="2"><li><code>listChannels</code></li></ol><p>This provides the active channel inventory for the operator.</p><ol start="3"><li><code>listPeers</code></li></ol><p>This provides the current peer set.</p><ol start="4"><li><code>describeGraph</code></li></ol><p>This provides the public network graph snapshot used by graph views and opening analysis.</p><ol start="5"><li><code>forwardingHistory</code></li></ol><p>This provides forwarding events used for routing performance and fee context.</p><ol start="6"><li><code>queryMissionControl</code></li></ol><p>This provides routing reliability signals used for peer evaluation.</p><ol start="7"><li><code>getNodeMetrics</code></li></ol><p>This provides node metric sets, including betweenness centrality when available.</p><h2 id="why-lnc-matters-here" tabindex="-1">Why LNC matters here <a class="header-anchor" href="#why-lnc-matters-here" aria-label="Permalink to &quot;Why LNC matters here&quot;">​</a></h2><ol><li><p>It keeps the source of truth close to the operator node.</p></li><li><p>It supports live reads instead of stale exports.</p></li><li><p>It reduces integration friction for a Lightning operator already running LND.</p></li><li><p>It makes it practical to separate local node reads from later private recommendation payloads.</p></li></ol><h2 id="lnc-and-privacy" tabindex="-1">LNC and privacy <a class="header-anchor" href="#lnc-and-privacy" aria-label="Permalink to &quot;LNC and privacy&quot;">​</a></h2><p>LNC gives LN Advisor access to raw node data. That is useful, but it creates a clear obligation.</p><ol><li><p>Raw node data should be read locally when the screen needs it.</p></li><li><p>Recommendation routes should not send raw telemetry when a reduced structure is enough.</p></li><li><p>The application should make the request boundary visible to the operator.</p></li></ol><p>This is why LNC sits at the start of the private pipeline. It is the trusted ingress point for the current product.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("lnc.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const lnc = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  lnc as default
};
