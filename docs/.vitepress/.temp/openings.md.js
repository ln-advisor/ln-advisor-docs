import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Openings","description":"","frontmatter":{},"headers":[],"relativePath":"openings.md","filePath":"openings.md","lastUpdated":1773788451000}');
const _sfc_main = { name: "openings.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="openings" tabindex="-1">Openings <a class="header-anchor" href="#openings" aria-label="Permalink to &quot;Openings&quot;">​</a></h1><p>Opening Recommendations ranks candidate peers for new channels. The screen answers a different question from the fee page. It is not about current policy on current channels. It is about where new connectivity is likely to help next.</p><h2 id="what-the-screen-uses" tabindex="-1">What the screen uses <a class="header-anchor" href="#what-the-screen-uses" aria-label="Permalink to &quot;What the screen uses&quot;">​</a></h2><p>The openings screen requires a wider network view than the fee screen.</p><ol><li><p>Node info</p></li><li><p>Existing channels</p></li><li><p>Peers</p></li><li><p>Full graph nodes</p></li><li><p>Full graph edges</p></li><li><p>Mission control pairs</p></li><li><p>Betweenness centrality metrics</p></li></ol><h2 id="why-these-inputs-matter" tabindex="-1">Why these inputs matter <a class="header-anchor" href="#why-these-inputs-matter" aria-label="Permalink to &quot;Why these inputs matter&quot;">​</a></h2><ol><li><p>Existing channels define what the operator already has.</p></li><li><p>Graph structure defines what the operator could reach.</p></li><li><p>Mission control adds recent routing reliability that raw graph structure cannot provide.</p></li><li><p>Centrality helps find nodes that sit in useful network positions.</p></li></ol><h2 id="current-local-preparation-flow" tabindex="-1">Current local preparation flow <a class="header-anchor" href="#current-local-preparation-flow" aria-label="Permalink to &quot;Current local preparation flow&quot;">​</a></h2><p>The openings page runs a direct and explicit path.</p><ol><li><p>It collects graph, channel, peer, mission control, and metric data.</p></li><li><p>It calls <code>normalizeSnapshot</code> on the full set.</p></li><li><p>It calls <code>applyPrivacyPolicy(normalizedSnapshot, &#39;feature_only&#39;)</code>.</p></li><li><p>It stores the normalized state and privacy transformed payload for request review.</p></li><li><p>It sends the reduced payload to <code>/api/recommend/channel-openings</code> in standard mode.</p></li><li><p>It can send the same reduced payload to the verified runtime path when that mode is enabled.</p></li></ol><h2 id="what-normalization-adds-for-openings" tabindex="-1">What normalization adds for openings <a class="header-anchor" href="#what-normalization-adds-for-openings" aria-label="Permalink to &quot;What normalization adds for openings&quot;">​</a></h2><p>Normalization builds a <code>potentialPeers</code> list. Each potential peer carries:</p><ol><li><p>Peer pubkey</p></li><li><p>Alias</p></li><li><p>Capacity</p></li><li><p>Channel count</p></li><li><p>Betweenness centrality</p></li><li><p>Mission success rate</p></li><li><p>Mission failure rate</p></li><li><p>Last activity timestamp</p></li></ol><p>Potential peers exclude the current node and peers that already have a channel with the operator.</p><h2 id="what-the-private-payload-keeps" tabindex="-1">What the private payload keeps <a class="header-anchor" href="#what-the-private-payload-keeps" aria-label="Permalink to &quot;What the private payload keeps&quot;">​</a></h2><p>The <code>feature_only</code> transform keeps enough structure for ranking while removing raw identifiers.</p><ol><li><p>Existing channels are represented by references and ratios.</p></li><li><p>Peer aggregates preserve activity, revenue, reliability, and centrality.</p></li><li><p>Potential peers preserve capacity, channel count, centrality, and mission signals through anonymized peer references.</p></li><li><p>Node totals preserve broad operating context.</p></li></ol><h2 id="recommendation-output" tabindex="-1">Recommendation output <a class="header-anchor" href="#recommendation-output" aria-label="Permalink to &quot;Recommendation output&quot;">​</a></h2><p>Opening output is emitted as <code>channelOpeningRecommendations</code>.</p><p>Each item carries:</p><ol><li><p><code>peerRef</code></p></li><li><p><code>score</code></p></li><li><p><code>confidence</code></p></li><li><p>signal bands for centrality, reliability, and capacity</p></li><li><p>a reason set</p></li></ol><p>The UI maps the anonymized result back into the local normalized snapshot so the operator can see useful labels and context on screen.</p><h2 id="verified-runtime-path" tabindex="-1">Verified runtime path <a class="header-anchor" href="#verified-runtime-path" aria-label="Permalink to &quot;Verified runtime path&quot;">​</a></h2><p>When verified runtime mode is enabled the openings screen can run the same reduced payload through the verified service. The page then displays:</p><ol><li><p>verification status</p></li><li><p>signer identity summary</p></li><li><p>signer provider type</p></li><li><p>runtime quote verification status</p></li><li><p>measurement policy status</p></li><li><p>live verification policy status</p></li></ol><h2 id="why-openings-use-the-private-pipeline" tabindex="-1">Why openings use the private pipeline <a class="header-anchor" href="#why-openings-use-the-private-pipeline" aria-label="Permalink to &quot;Why openings use the private pipeline&quot;">​</a></h2><p>Opening analysis needs broad graph context and internal routing signals. That is useful for ranking, but it does not justify sending raw node identifiers and raw balances when references, ratios, and aggregates are enough. The opening screen is therefore the clearest example of the product goal, keep ranking power, reduce disclosure.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("openings.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const openings = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  openings as default
};
